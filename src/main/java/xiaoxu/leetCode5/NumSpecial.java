package xiaoxu.leetCode5;

/**
 * @author xx
 * @create 2021/2/22 11:01
 */
public class NumSpecial{

    /**
     * 给你一个大小为 rows x cols 的矩阵 mat，
     * 其中 mat[i][j] 是 0 或 1，请返回 矩阵 mat 中特殊位置的数目 。
     * 特殊位置 定义：如果 mat[i][j] == 1 并且第 i 行和
     * 第 j 列中的所有其他元素均为 0（行和列的下标均 从 0 开始 ），则位置 (i, j) 被称为特殊位置。
     * 输入：mat = [[1,0,0],
     *             [0,0,1],
     *             [1,0,0]]
     * 输出：1
     * 解释：(1,2) 是一个特殊位置，因为 mat[1][2] == 1 且所处的行和列上所有其他元素都是 0
     *
     */
    public int numSpecial(int[][] mat) {
        return -1;
    }
}
